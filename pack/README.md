# example

This pack's job is to install the mandatory DC Assurance CPU and memory monitoring probe on all blueprints.

When onboarding an edge to Data Centre Assurance, you may see an error post pre-flight checks that says `Stream CPU and Memory Utilization Probe` is missing. By running this pack, it will install this probe on all blueprints within your Apstra DC Director instance. 
